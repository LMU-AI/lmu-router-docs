---
title: Key 安全
description: 灵眸 API Key IP 白名单 / 黑名单配置指南：限制可调用 IP、防止 Key 泄漏被盗刷，支持单 IP 与 CIDR 网段，Claude API / Claude Code / OpenAI / 国产模型通用。
ogDescription: 灵眸 AI Key 安全防护：给 Claude / GPT / 国产模型 API Key 配置 IP 白名单与黑名单，支持单 IP、CIDR 网段与 IPv6，避免 Key 泄漏后被他人盗用消耗额度，附排错与生产环境最佳实践。
keywords:
  - API Key 安全
  - API Key IP 白名单
  - API Key IP 黑名单
  - Claude API Key 保护
  - Claude API Key 泄漏
  - Claude Code Key 安全
  - Claude Code Key 泄漏
  - Anthropic Key IP 限制
  - API Key 被盗刷
  - API Key 防盗刷
  - Key 泄漏怎么办
  - IP 限制
  - CIDR 白名单
  - IPv6 白名单
  - 灵眸 Key 安全
  - 灵眸 AI Key IP 白名单
  - 403 ACCESS_DENIED
alternateNames:
  - Claude API Key IP 白名单配置
  - 灵眸 AI Key 安全防护指南
  - API Key 防盗刷设置
  - Key 泄漏后如何止损
  - Claude Code Key 保护教程
faq:
  - q: IP 白名单和黑名单可以同时用吗？
    a: 可以。同时配置时，黑名单先生效——即使 IP 在白名单里，只要命中黑名单也会被拒绝。
  - q: 白名单填了内网 IP 为什么访问不了？
    a: 内网 IP（如 192.168.x.x、10.x.x.x）灵眸服务端看不到，只能识别你的**公网出口 IP**。请用 `curl https://ifconfig.me` 查出真实出口 IP 后再填。
  - q: 家里 IP 会变，怎么用白名单？
    a: 如果是动态公网 IP，可以填运营商分配的 CIDR 网段（如 `123.45.0.0/16`）放宽范围；若无法确定网段，建议不启用白名单，改用其它保护手段（限流、总额度、模型白名单）。
  - q: 灵眸 API Key 泄漏了怎么办？
    a: 立刻在控制台**重置或删除**该 Key；如果已配 IP 白名单，泄漏的 Key 在陌生 IP 上无法调用，可以显著降低损失。建议同时排查泄漏来源（公开仓库提交、日志、聊天记录、截图等），并对新 Key 直接开启 IP 白名单。
  - q: 一把 Key 可以配几个 IP？支持 IPv6 吗？
    a: 每行一条规则，可混填单 IP 与 CIDR 网段，条目数量没有强限制。IPv4 和 IPv6 都支持（IPv6 示例：`2001:db8::/32`）。若你同时有 v4 / v6 出口，两个都要填。
  - q: 生产服务器调用要怎么配白名单？
    a: 填服务器的**公网 IP**（不是内网 IP）。若用了负载均衡或存在多个出口 IP，把所有可能的出口 IP 或所在 CIDR 段一起加入白名单；本地开发建议用另一把不设白名单的 Key，避免互相干扰。
  - q: IP 白名单和限流、总额度冲突吗？
    a: 不冲突，建议叠加使用。IP 白名单限"谁能用"、限流限"用多快"、总额度限"用多少"。生产 Key 三者一起配才能做到纵深防护——即使白名单被绕过，限流与额度也能兜底。
  - q: 调用被拒返回 403 ACCESS_DENIED 是什么原因？
    a: 大概率是 IP 未命中白名单。用 `curl https://ifconfig.me` 查真实出口 IP，对照 Key 的白名单，把当前 IP 加入或临时清空白名单测试；若仍被拒，检查是否命中黑名单、Key 是否停用、额度是否耗尽。
---

Key 一旦泄漏，可能被他人盗用消耗你的额度。**IP 白名单**是最直接的防护——只允许你信任的 IP 使用这把 Key。

## 在哪里配置

**控制台 → API Keys → 编辑（或新建）**，找到 **IP 白名单** 和 **IP 黑名单** 两个输入框。

每行一条规则，支持两种格式：

| 格式 | 示例 | 说明 |
|------|------|------|
| 单个 IP | `203.0.113.10` | 精确匹配一个 IP |
| CIDR 网段 | `203.0.113.0/24` | 匹配一整段 IP（`/24` = 256 个 IP） |

IPv4 和 IPv6 都支持（IPv6 示例：`2001:db8::/32`）。

## 生效规则

- **白名单为空** → 不限制来源，任何 IP 都能用
- **白名单非空** → 只有列表内的 IP 能用，其它一律拒绝
- **黑名单** → 命中即拒绝，**优先级高于白名单**
- 两者可以同时用（先查黑名单再查白名单）

被拒绝时，接口返回 `403 ACCESS_DENIED`。

## 找到你的真实出口 IP（关键！）

**这是最容易踩的坑**：你在 Key 上配的 IP，必须是灵眸服务器看到的**公网出口 IP**，不是你电脑上 `ipconfig` / `ifconfig` 显示的内网地址。

```bash
# 命令行
curl https://ifconfig.me
# 或
curl https://ip.sb
```

或者在浏览器打开 [ip.sb](https://ip.sb) / [ifconfig.me](https://ifconfig.me) 查看。

<Callout type="warn">
  开了 VPN、公司代理、移动网络切换 Wi-Fi 时，出口 IP 会变。配白名单前先用上面的命令确认一次。
</Callout>

## 常见场景配法

**场景 1：固定办公 IP**
```
白名单：
203.0.113.10
```

**场景 2：家 + 公司两处办公**
```
白名单：
203.0.113.10      # 公司出口
198.51.100.20     # 家里宽带
```

**场景 3：跑在自己的云服务器上**
```
白名单：
<你云服务器的公网 IP>
```
只允许你的服务器调用，本地开发用另一把 Key（不设白名单）。

**场景 4：只是想临时封掉某个可疑 IP**
```
黑名单：
1.2.3.4
```
其它照常，不影响正常使用。

## 排错

**问：调用返回 403 ACCESS_DENIED，是不是 IP 限制？**

大概率是。请依次检查：

1. 用 `curl https://ifconfig.me` 确认当前真实出口 IP
2. 对照 Key 的白名单看是否匹配
3. 如果不匹配，把当前 IP 加入白名单，或临时清空白名单测试
4. 依旧被拒，检查是否在黑名单里、Key 是否已停用、额度是否耗尽

**问：白名单填了 IP 但依然被拒？**

- 填的是**内网 IP**（`192.168.x.x` / `10.x.x.x` / `172.16-31.x.x`）？换成公网出口 IP。
- 填的是 IPv6 但你用 IPv4 出网（或反之）？两个都填上。
- 填了 CIDR 但格式错误（如 `1.2.3.4/33`）？错误规则会被静默忽略。

## 建议做法

- **生产用的 Key** 强烈建议配 IP 白名单，把攻击面缩到最小
- **开发/调试的 Key** 可以不配白名单，但一定要设**总额度上限**兜底
- 泄漏担忧较大时，配合 [**限流**、**总额度**、**模型白名单**](./how-to-use-token) 一起用，层层设防
- Key 一旦怀疑泄漏，第一时间在控制台**重置或删除**该 Key
