---
title: Безопасность ключа
description: "Руководство по белому/чёрному спискам IP для API-ключа LMU AI: ограничьте, какие IP могут использовать ваш ключ, чтобы утёкший ключ не был использован во вред; поддерживаются одиночные IP и диапазоны CIDR."
ogDescription: "Безопасность ключа LMU AI: настройте белые и чёрные списки IP для ваших API-ключей Claude / GPT / китайских моделей с поддержкой одиночных IP, диапазонов CIDR и IPv6, чтобы утёкший ключ не мог быть использован другими для исчерпания вашей квоты — включает устранение неполадок и рекомендации для продакшена."
keywords:
  - безопасность API-ключа
  - белый список IP для API-ключа
  - предотвращение злоупотребления API-ключом
  - что делать при утечке API-ключа Claude
  - настройка белого списка CIDR
  - белый список IPv6
  - 403 ACCESS_DENIED
  - безопасность ключа Claude Code
  - ограничение IP для ключа LMU AI
  - защита API-ключа AI
alternateNames:
  - настройка белого списка IP для API-ключа Claude
  - руководство по безопасности ключа LMU AI
  - настройки защиты от злоупотребления API-ключом
  - Как ограничить убытки после утечки ключа
  - руководство по защите ключа Claude Code
faq:
  - q: Можно ли использовать белый и чёрный списки IP одновременно?
    a: Да. Когда настроены оба, чёрный список срабатывает первым — даже если IP находится в белом списке, он всё равно будет отклонён, если совпадает с чёрным списком.
  - q: Почему я не могу подключиться, когда добавляю приватный (локальный) IP в белый список?
    a: Серверы LMU AI не видят приватные IP (например, 192.168.x.x или 10.x.x.x); они видят только ваш **публичный исходящий IP**. Используйте `curl https://ifconfig.me`, чтобы узнать ваш реальный исходящий IP, и введите именно его.
  - q: Мой домашний IP меняется — как использовать белый список?
    a: Если у вас динамический публичный IP, вы можете ввести диапазон CIDR, назначенный вашим провайдером (например, `123.45.0.0/16`), чтобы расширить диапазон; если вы не можете определить диапазон, мы рекомендуем не включать белый список, а использовать другие меры защиты (ограничение частоты, общую квоту, белый список моделей).
  - q: Что делать, если мой API-ключ LMU AI утёк?
    a: Немедленно **сбросьте или удалите** ключ в консоли; если белый список IP уже настроен, утёкший ключ нельзя будет использовать с незнакомых IP, что значительно снижает ваши убытки. Мы также рекомендуем выяснить источник утечки (коммиты в публичных репозиториях, логи, история чатов, скриншоты и т. д.) и сразу же включить белый список IP на новом ключе.
  - q: Сколько IP может иметь один ключ? Поддерживается ли IPv6?
    a: "По одному правилу на строку; вы можете сочетать одиночные IP и диапазоны CIDR, жёсткого ограничения на количество записей нет. Поддерживаются и IPv4, и IPv6 (пример IPv6: `2001:db8::/32`). Если у вас есть исходящие адреса как v4, так и v6, введите оба."
  - q: Как настроить белый список для вызовов с продакшен-сервера?
    a: Введите **публичный IP** сервера (не его приватный IP). Если вы используете балансировщик нагрузки или у вас несколько исходящих IP, добавьте в белый список все возможные исходящие IP или их диапазоны CIDR; для локальной разработки используйте отдельный ключ без белого списка, чтобы избежать помех.
  - q: Конфликтуют ли белый список IP, ограничение частоты и общая квота?
    a: Нет, и мы рекомендуем сочетать их. Белый список IP ограничивает «кто может использовать», ограничение частоты ограничивает «как быстро», а общая квота ограничивает «сколько». Настройте все три на продакшен-ключе для эшелонированной защиты — даже если белый список обойдут, ограничение частоты и квота всё равно послужат подстраховкой.
  - q: Почему мой вызов отклонён с ошибкой 403 ACCESS_DENIED?
    a: Скорее всего, IP не совпал с белым списком. Используйте `curl https://ifconfig.me`, чтобы проверить ваш реальный исходящий IP, сравните его с белым списком ключа и либо добавьте текущий IP, либо временно очистите белый список для проверки; если он всё равно отклоняется, проверьте, не совпадает ли он с чёрным списком, не отключён ли ключ и не исчерпана ли квота.
---

Если ключ утекает, другие могут использовать его для расходования вашей квоты. **Белый список IP** — самая прямая защита: только доверенным IP разрешено использовать ключ.

## Где это настроить

Перейдите в **консоль → API-ключи → Редактировать (или Создать)** и найдите два поля: **Белый список IP** и **Чёрный список IP**.

По одному правилу на строку, в одном из двух форматов:

| Формат | Пример | Описание |
|------|------|------|
| Одиночный IP | `203.0.113.10` | Совпадает ровно с одним IP |
| Диапазон CIDR | `203.0.113.0/24` | Совпадает с целым диапазоном IP (`/24` = 256 IP) |

Поддерживаются и IPv4, и IPv6 (пример IPv6: `2001:db8::/32`).

## Как это работает

- **Белый список пуст** → нет ограничений по источнику; любой IP может использовать ключ
- **Белый список не пуст** → только IP из списка могут его использовать; все остальные отклоняются
- **Чёрный список** → совпадающие IP отклоняются, **имея приоритет над белым списком**
- Вы можете использовать оба одновременно (сначала проверяется чёрный список, затем белый)

При отклонении API возвращает `403 ACCESS_DENIED`.

## Узнайте ваш реальный исходящий IP (важно!)

**Это самая частая ошибка**: IP, который вы указываете в ключе, должен быть **публичным исходящим IP**, который видят серверы LMU AI, а не приватным адресом, который показывает `ipconfig` / `ifconfig` на вашем компьютере.

```bash
# Command line
curl https://ifconfig.me
# or
curl https://ip.sb
```

Или откройте [ip.sb](https://ip.sb) / [ifconfig.me](https://ifconfig.me) в браузере.

<Callout type="warn">
  Ваш исходящий IP меняется, когда вы включаете VPN или корпоративный прокси, или когда ваше мобильное устройство переключает сети Wi-Fi. Подтвердите его с помощью команды выше перед настройкой белого списка.
</Callout>

## Типичные сценарии

**Сценарий 1: Фиксированный офисный IP**
```
Whitelist:
203.0.113.10
```

**Сценарий 2: Работа и из дома, и из офиса**
```
Whitelist:
203.0.113.10      # Company egress
198.51.100.20     # Home broadband
```

**Сценарий 3: Работа на собственном облачном сервере**
```
Whitelist:
<your cloud server's public IP>
```
Разрешите вызывать API только вашему серверу, а для локальной разработки используйте отдельный ключ (без белого списка).

**Сценарий 4: Вы просто хотите временно заблокировать подозрительный IP**
```
Blacklist:
1.2.3.4
```
Всё остальное работает как обычно, без влияния на нормальное использование.

## Устранение неполадок

**В: Мой вызов возвращает 403 ACCESS_DENIED — это ограничение по IP?**

Скорее всего. Проверьте по порядку:

1. Используйте `curl https://ifconfig.me`, чтобы подтвердить ваш текущий реальный исходящий IP
2. Сравните его с белым списком ключа, чтобы увидеть, совпадает ли он
3. Если не совпадает, добавьте текущий IP в белый список или временно очистите белый список для проверки
4. Если он всё равно отклоняется, проверьте, не в чёрном ли он списке, не отключён ли ключ и не исчерпана ли квота

**В: Я добавил IP в белый список, но меня всё равно отклоняют?**

- Вы ввели **приватный IP** (`192.168.x.x` / `10.x.x.x` / `172.16-31.x.x`)? Замените его на ваш публичный исходящий IP.
- Вы ввели адрес IPv6, но выходите в сеть по IPv4 (или наоборот)? Введите оба.
- Вы ввели CIDR с некорректным форматом (например, `1.2.3.4/33`)? Недействительные правила молча игнорируются.

## Рекомендуемые практики

- Для **продакшен-ключей** мы настоятельно рекомендуем настроить белый список IP, чтобы минимизировать поверхность атаки
- Для **ключей разработки / отладки** вы можете обойтись без белого списка, но обязательно установите **лимит общей квоты** в качестве подстраховки
- Если вы особенно беспокоитесь об утечках, сочетайте это с [**ограничением частоты**, **общей квотой** и **белым списком моделей**](/ru/docs/guide/how-to-use-token) для эшелонированной защиты
- Как только вы заподозрите утечку ключа, немедленно **сбросьте или удалите** его в консоли
