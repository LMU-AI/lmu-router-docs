---
title: بنية الموثوقية
description: "بنية موثوقية الذكاء الاصطناعي لدى LMU AI: التوجيه متعدد المصادر، فحوصات السلامة، والتبديل التلقائي عند الفشل على ثلاث طبقات؛ هدف توفّر 99.5% على خطتي Standard وEnterprise."
ogDescription: "مُرحِّل API مستقر وموثوق لـ Claude: تستخدم LMU AI التوجيه متعدد المصادر، والتبديل عند الفشل على ثلاث طبقات (رسمي / مُنبع / احتياطي)، وفحص السلامة لتقديم خدمة مُرحِّل API عالية التوفّر ومنخفضة الكمون لـ Claude / GPT / Gemini."
keywords:
  - مُرحِّل Claude API مستقر
  - توفّر عالٍ لـ Claude API
  - التبديل عند الفشل لـ Claude API
  - ماذا تفعل عندما يكون Claude API غير مستقر
  - اتفاقية مستوى الخدمة لمُرحِّل AI API
  - التوجيه متعدد المصادر
  - مُرحِّل Claude في الصين
  - موثوقية مُرحِّل API للنماذج الكبيرة
  - مُرحِّل AI API موثوق
  - مُرحِّل Claude Code مستقر
alternateNames:
  - نظرة عامة على موثوقية مُرحِّل Claude API
  - بنية موثوقية LMU AI
  - مُرحِّل عالي التوفّر لـ Claude / GPT / Gemini
faq:
  - q: هل مُرحِّل Claude API لدى LMU AI مستقر؟
    a: تستخدم LMU AI بنية تبديل عند الفشل على ثلاث طبقات (القناة الرسمية ← القناة المُنبع ← القناة الاحتياطية)، وتُجري باستمرار فحوصات سلامة على كل مُنبع؛ يتم تلقائيًا تقليل وزن القنوات غير السليمة أو إزالتها، وتكون أعطال النقطة الواحدة شفافة للمستخدمين النهائيين، وهذا يُبقي Claude / GPT / Gemini API عالي التوفّر ومنخفض الكمون.
  - q: لماذا يكون الاتصال المباشر بـ Claude API الرسمي غير مستقر؟
    a: كثيرًا ما يجلب الاتصال المباشر بالـ API الرسمي تقييدًا إقليميًا للمعدل، وضوابط مخاطر على الحسابات، ومشكلات توافق أثناء تبديل الإصدارات، وارتفاعات في كمون أول رمز (token)، وغير ذلك. وإذا كان المُرحِّل مجرد وكيل بسيط، فإن هذه المشكلات تُمرَّر كما هي. تُصمِّم LMU AI نظامها على فرضية أن "المصادر المُنبع غير مستقرة"، فتُخفي التذبذب عبر تجميع متعدد المصادر، وإعادات المحاولة التلقائية، والتوجيه المُتدرِّج.
  - q: كيف يختلف مُرحِّل LMU AI عن موقع مرآة عادي؟
    a: عادةً ما يكون موقع المرآة العادي عبارة عن مصدر مُنبع واحد إضافةً إلى وكيل شفّاف؛ أما LMU AI فتربط عدة قنوات مستقلة خلف النموذج نفسه وتختار مصدرًا مُنبع ديناميكيًا بناءً على طبقة خطتك، والنموذج، والسلامة، والحمل، مع توفير صفحة حالة عامة، وإعادات محاولة تلقائية عند الأخطاء، وتمرير التدفّق (streaming passthrough)، واتساق الفوترة.
---

نتعامل مع "الاستقرار" باعتباره الميزة رقم واحد للمنتج. تشرح هذه الصفحة كيف نحقق ذلك، وماذا يحدث عندما تسوء الأمور.

## لماذا تُعدّ الموثوقية صعبة إلى هذا الحد؟

يواجه المطورون الذين يتصلون مباشرةً بالـ API الرسمي أمورًا مثل:

- تقييد إقليمي للمعدل / عدم التوفّر
- حظر قناة دفع واحدة أو حساب واحد
- مشكلات توافق أثناء تبديل إصدارات النماذج
- الاصطفاف في ساعات الذروة وارتفاعات في كمون أول رمز (token)

إذا كانت خدمة المُرحِّل مجرد وكيل بسيط، فإن هذه المشكلات **تُمرَّر كما هي** إلى المستخدمين، أو حتى تتفاقم. نهجنا هو تصميم النظام على فرضية أن "المصادر المُنبع غير مستقرة"، بدلًا من التعامل مع ذلك باعتباره استثناءً.

## بنية التوجيه متعدد المصادر لدينا

لكل استدعاء API، تُعالَج الطلبات عبر المسار التالي:

<Mermaid chart={`flowchart TD
    U([User request]) --> E[Edge gateway<br/>Auth / rate limiting / parameter validation]
    E --> R[Routing decision layer<br/>Select upstream by plan tier / model / health / load]

    R -->|First priority| O1[Official channel A primary]
    R -->|First priority| O2[Official channel B backup]
    R -.->|When official is unavailable| P1[Upstream channel A]
    R -.->|When official is unavailable| P2[Upstream channel B]
    R -.->|When the first two tiers are unavailable| F1[Fallback channel A]
    R -.->|When the first two tiers are unavailable| F2[Fallback channel B]

    O1 --> A[Response aggregation layer<br/>Error retries / streaming passthrough / billing]
    O2 --> A
    P1 --> A
    P2 --> A
    F1 --> A
    F2 --> A
    A --> Ret([Return to user])
`} />

**الآليات الأساسية:**

- **مجمّع متعدد المصادر المُنبع**: تجلس عدة قنوات مستقلة خلف النموذج نفسه، ما يتجنّب أعطال النقطة الواحدة
- **فحص السلامة**: نراقب باستمرار معدل نجاح كل مصدر مُنبع، وكمون أول رمز (token)، ومعدل تقييد المعدل، و**يتم تلقائيًا تقليل وزن القنوات غير السليمة أو إزالتها**
- **التبديل التلقائي عند الفشل**: عندما تُعيد القناة الرئيسية 5xx / تقييد معدل / انتهاء مهلة، يتحوّل الطلب إلى قناة احتياطية في غضون أجزاء من الثانية، **عادةً دون أي تأثير من جانبك**
- **إعادة اتصال التدفّق عند الانقطاع**: عندما ينقطع استجابة تدفّق في منتصفها، نحاول استئنافها على البروتوكولات التي تدعم ذلك

## مستوى الخدمة (SLA)

تعهداتنا الخارجية مبنية على "ما يمكننا تقديمه فعليًا" — لا نطلق وعودًا فارغة.

| الطبقة | هدف التوفّر | التقرير الشهري | عقد العمل |
|------|-----------|---------|---------|
| Economy | أفضل جهد ممكن | — | غير قابل للتطبيق |
| Standard | 99.5% | — | غير قابل للتطبيق |
| Enterprise | 99.5% | مُقدَّم | تتوفّر شروط SLA أعلى وشروط تعويض |

> طبقة Economy مخصّصة للمطورين الأفراد الذين يريدون أفضل قيمة ويمكنهم تحمّل تذبذبات عرضية؛ إذا كان لعملك متطلبات موثوقية صارمة، فاختَر طبقة Enterprise أو اتصل بفريق المبيعات للحصول على خطة مخصّصة.

كيف يُقاس التوفّر:
- نحسب فقط معدل نجاح الطلبات "من جانب LMU AI"، **باستثناء مشكلات الشبكة من جانب المستخدم، وأخطاء المعاملات، ورفض المحتوى من نموذج المصدر المُنبع نفسه**
- عطل واحد يستمر أقل من 60 ثانية لا يُحتسب
- يتضمّن التقرير الشهري معدلات النجاح وكمون P95 مُقسّمة حسب النموذج وحسب الفترة الزمنية

## ماذا يحدث أثناء الحادث

**تذبذب قصير (< 5 دقائق)**
- تبدّل طبقة التوجيه المصادر المُنبع تلقائيًا، دون إعلان
- تُسجّل [صفحة الحالة](https://api.lmuai.com/monitor) الحدث

**انقطاع مصدر مُنبع واحد (< ساعة واحدة)**
- تُكمل طبقة التوجيه التبديل؛ وإذا كان مرئيًا لبعض المستخدمين/النماذج، فإننا ننشره على صفحة الحالة
- يُخطَر مستخدمو Standard وEnterprise عبر البريد الإلكتروني

**تأثير إقليمي أو تأثير متزامن على عدة مصادر مُنبع**
- يُثبَّت الحدث على صفحة الحالة مع تحديثات دورية
- يحصل مستخدمو Enterprise على تحديثات تقدّم عبر البريد الإلكتروني في الوقت الفعلي
- يُنشر تقرير ما بعد الحادث في غضون 48 ساعة بعد انتهاء الحدث

## ما لا نفعله

لتجنّب تضليل المستخدمين، نحن حاليًا **لا** نَعِد بما يلي:

- **تبديل سلس بلا كمون**: يُدخِل التبديل عند الفشل كمونًا إضافيًا يتراوح من أجزاء من الثانية إلى ثوانٍ، لذا لن يكون سلسًا تمامًا أبدًا
- **تعويض توافق عبر النماذج**: إذا تم سحب نموذج مصدر مُنبع نفسه من الخدمة (على سبيل المثال، إيقاف مزوّد لإصدار نموذج)، فإننا نعلن عن ذلك بدلًا من التبديل بصمت إلى نموذج مختلف
- **إعادات محاولة غير محدودة**: تُعاد محاولة الطلبات الفاشلة عددًا محدودًا من المرات وفق السياسة، حتى لا تدفع مقابل استدعاءات مكررة غير متوقعة

## الأسئلة الشائعة

<Callout type="info" title="لماذا لا أزال أواجه أعطالًا أحيانًا؟">

لا يوجد نظام متوفّر بنسبة 100%. هدفنا هو إبقاء التوفّر الإجمالي أعلى من 99.5% والتعافي بسرعة عند حدوث الأعطال. إذا واجهت أعطالًا مستمرة (على سبيل المثال، فشل النوع نفسه من الطلبات عدة مرات خلال 5 دقائق)، يُرجى الاتصال بالدعم مع معرّف الطلب (request ID) وسنُعطي التحقيق أولوية.

</Callout>

<Callout type="info" title="كيف أتقدّم بطلب لطبقة Enterprise؟">

راجِع صفحة [خطط Enterprise](/ar/docs/enterprise)، أو اتصل بالمبيعات مباشرةً:

- البريد الإلكتروني: business@lmuai.com
- WeChat / الهاتف: 18599001010

</Callout>
