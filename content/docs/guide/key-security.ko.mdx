---
title: 키 보안
description: "LMU AI API 키 IP 화이트리스트/블랙리스트 가이드: 어떤 IP가 키를 호출할 수 있는지 제한하여 유출된 키가 악용되는 것을 방지합니다. 단일 IP 및 CIDR 범위를 지원합니다."
ogDescription: "LMU AI 키 보안: Claude / GPT / 중국 모델 API 키에 IP 화이트리스트와 블랙리스트를 구성하며, 단일 IP, CIDR 범위, IPv6를 지원하여 유출된 키를 타인이 사용해 할당량을 소진하지 못하도록 합니다 — 문제 해결 및 프로덕션 모범 사례 포함."
keywords:
  - API 키 보안
  - API 키 IP 화이트리스트
  - API 키 악용 방지
  - Claude API 키가 유출되었을 때 대처법
  - CIDR 화이트리스트 구성
  - IPv6 화이트리스트
  - 403 ACCESS_DENIED
  - Claude Code 키 보안
  - LMU AI 키 IP 제한
  - AI API 키 보호
alternateNames:
  - Claude API 키 IP 화이트리스트 구성
  - LMU AI 키 보안 가이드
  - API 키 악용 방지 설정
  - 키 유출 후 손실을 줄이는 방법
  - Claude Code 키 보호 튜토리얼
faq:
  - q: IP 화이트리스트와 블랙리스트를 동시에 사용할 수 있나요?
    a: 네. 둘 다 구성된 경우 블랙리스트가 먼저 적용됩니다 — 어떤 IP가 화이트리스트에 있더라도 블랙리스트에 일치하면 여전히 거부됩니다.
  - q: 화이트리스트에 사설(LAN) IP를 넣었는데 왜 연결이 안 되나요?
    a: "LMU AI의 서버는 사설 IP(예: 192.168.x.x 또는 10.x.x.x)를 볼 수 없습니다. 서버는 오직 여러분의 **공인 송출 IP**만 봅니다. `curl https://ifconfig.me`를 사용해 실제 송출 IP를 확인한 다음 그 값을 입력하세요."
  - q: 집 IP가 바뀌는데 화이트리스트를 어떻게 사용하나요?
    a: "동적 공인 IP가 있는 경우 ISP가 할당한 CIDR 범위(예: `123.45.0.0/16`)를 입력해 범위를 넓힐 수 있습니다. 범위를 확정할 수 없다면 화이트리스트를 활성화하지 말고 다른 보호 수단(속도 제한, 총 할당량, 모델 화이트리스트)을 사용하는 것을 권장합니다."
  - q: LMU AI API 키가 유출되면 어떻게 해야 하나요?
    a: 즉시 콘솔에서 키를 **재설정하거나 삭제**하세요. IP 화이트리스트가 이미 구성되어 있다면 유출된 키는 낯선 IP에서 사용할 수 없어 손실을 크게 줄일 수 있습니다. 또한 유출 경로(공개 저장소 커밋, 로그, 채팅 기록, 스크린샷 등)를 추적하고 새 키에 즉시 IP 화이트리스트를 활성화하는 것을 권장합니다.
  - q: 하나의 키에 IP를 몇 개까지 넣을 수 있나요? IPv6를 지원하나요?
    a: "한 줄에 규칙 하나씩 입력하며, 단일 IP와 CIDR 범위를 혼합할 수 있고 항목 수에 하드 리밋은 없습니다. IPv4와 IPv6 모두 지원됩니다(IPv6 예: `2001:db8::/32`). v4와 v6 송출이 모두 있다면 둘 다 입력하세요."
  - q: 프로덕션 서버에서의 호출을 위한 화이트리스트는 어떻게 설정하나요?
    a: 서버의 **공인 IP**(사설 IP가 아님)를 입력하세요. 로드 밸런서를 사용하거나 여러 송출 IP가 있는 경우 가능한 모든 송출 IP 또는 해당 CIDR 범위를 화이트리스트에 추가하세요. 로컬 개발에는 간섭을 피하기 위해 화이트리스트가 없는 별도의 키를 사용하세요.
  - q: IP 화이트리스트, 속도 제한, 총 할당량이 서로 충돌하나요?
    a: 아니요, 오히려 함께 쌓아서 사용하는 것을 권장합니다. IP 화이트리스트는 "누가 사용할 수 있는지"를 제한하고, 속도 제한은 "얼마나 빠르게"를, 총 할당량은 "얼마나 많이"를 제한합니다. 심층 방어를 위해 프로덕션 키에 세 가지를 모두 구성하세요 — 화이트리스트가 우회되더라도 속도 제한과 할당량이 여전히 백스톱을 제공합니다.
  - q: 왜 제 호출이 403 ACCESS_DENIED로 거부되나요?
    a: 대부분 IP가 화이트리스트와 일치하지 않았기 때문입니다. `curl https://ifconfig.me`를 사용해 실제 송출 IP를 확인하고, 키의 화이트리스트와 비교한 다음, 현재 IP를 추가하거나 화이트리스트를 임시로 비워서 테스트해 보세요. 그래도 거부된다면 블랙리스트와 일치하는지, 키가 비활성화되었는지, 할당량이 소진되었는지 확인하세요.
---

키가 유출되면 타인이 이를 사용해 여러분의 할당량을 소진할 수 있습니다. **IP 화이트리스트**는 가장 직접적인 보호 수단입니다 — 여러분이 신뢰하는 IP만 키를 사용할 수 있습니다.

## 어디서 구성하나요

**콘솔 → API 키 → 편집(또는 생성)**으로 이동하여 **IP 화이트리스트**와 **IP 블랙리스트**라는 두 필드를 찾으세요.

한 줄에 규칙 하나씩, 두 가지 형식 중 하나로 입력합니다:

| 형식 | 예시 | 설명 |
|------|------|------|
| 단일 IP | `203.0.113.10` | 정확히 하나의 IP와 일치 |
| CIDR 범위 | `203.0.113.0/24` | IP의 전체 범위와 일치(`/24` = 256개 IP) |

IPv4와 IPv6 모두 지원됩니다(IPv6 예: `2001:db8::/32`).

## 어떻게 적용되나요

- **화이트리스트 비어 있음** → 출처 제한 없음; 어떤 IP든 키를 사용할 수 있음
- **화이트리스트 비어 있지 않음** → 목록에 있는 IP만 사용할 수 있고, 나머지는 모두 거부됨
- **블랙리스트** → 일치하는 IP는 거부되며, **화이트리스트보다 우선함**
- 둘을 동시에 사용할 수 있습니다(블랙리스트가 먼저 확인되고 그다음 화이트리스트)

거부되면 API는 `403 ACCESS_DENIED`를 반환합니다.

## 실제 송출 IP 확인하기(중요!)

**이것이 가장 저지르기 쉬운 실수입니다**: 키에 구성하는 IP는 LMU AI의 서버가 보는 **공인 송출 IP**여야 하며, 여러분 컴퓨터의 `ipconfig` / `ifconfig`가 보여주는 사설 주소가 아닙니다.

```bash
# Command line
curl https://ifconfig.me
# or
curl https://ip.sb
```

또는 브라우저에서 [ip.sb](https://ip.sb) / [ifconfig.me](https://ifconfig.me)를 여세요.

<Callout type="warn">
  VPN이나 회사 프록시를 켜거나, 모바일 기기가 Wi-Fi 네트워크를 전환하면 송출 IP가 바뀝니다. 화이트리스트를 구성하기 전에 위 명령으로 확인하세요.
</Callout>

## 일반적인 시나리오

**시나리오 1: 고정된 사무실 IP**
```
Whitelist:
203.0.113.10
```

**시나리오 2: 집과 사무실 양쪽에서 작업**
```
Whitelist:
203.0.113.10      # Company egress
198.51.100.20     # Home broadband
```

**시나리오 3: 자체 클라우드 서버에서 실행**
```
Whitelist:
<your cloud server's public IP>
```
여러분의 서버만 API를 호출하도록 허용하고, 로컬 개발에는 별도의 키(화이트리스트 없음)를 사용하세요.

**시나리오 4: 의심스러운 IP를 잠시 차단하고 싶을 뿐**
```
Blacklist:
1.2.3.4
```
그 외 모든 것은 평소처럼 작동하며, 정상적인 사용에 영향을 주지 않습니다.

## 문제 해결

**Q: 제 호출이 403 ACCESS_DENIED를 반환합니다 — IP 제한 때문인가요?**

대부분 그렇습니다. 순서대로 확인하세요:

1. `curl https://ifconfig.me`를 사용해 현재 실제 송출 IP를 확인
2. 키의 화이트리스트와 비교해 일치하는지 확인
3. 일치하지 않으면 현재 IP를 화이트리스트에 추가하거나, 화이트리스트를 임시로 비워서 테스트
4. 그래도 거부된다면 블랙리스트에 있는지, 키가 비활성화되었는지, 할당량이 소진되었는지 확인

**Q: IP를 화이트리스트에 넣었는데도 거부됩니다?**

- **사설 IP**(`192.168.x.x` / `10.x.x.x` / `172.16-31.x.x`)를 입력하지 않았나요? 공인 송출 IP로 교체하세요.
- IPv6 주소를 입력했는데 IPv4로 접속하고 있나요(또는 그 반대)? 둘 다 입력하세요.
- 잘못된 형식의 CIDR(예: `1.2.3.4/33`)을 입력하지 않았나요? 유효하지 않은 규칙은 조용히 무시됩니다.

## 권장 사례

- **프로덕션 키**의 경우, 공격 표면을 최소화하기 위해 IP 화이트리스트를 구성하는 것을 강력히 권장합니다
- **개발 / 디버깅 키**의 경우 화이트리스트를 건너뛸 수 있지만, 백스톱으로 **총 할당량 상한**을 반드시 설정해야 합니다
- 유출이 특히 걱정된다면 [**속도 제한**, **총 할당량**, **모델 화이트리스트**](/ko/docs/guide/how-to-use-token)와 결합하여 계층적 방어를 구성하세요
- 키가 유출되었다고 의심되는 순간, 즉시 콘솔에서 키를 **재설정하거나 삭제**하세요
