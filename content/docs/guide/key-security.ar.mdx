---
title: أمان المفاتيح
description: "دليل القائمة البيضاء/السوداء لعناوين IP لمفاتيح واجهة LMU AI: قيّد عناوين IP التي يمكنها استدعاء مفتاحك لمنع إساءة استخدام مفتاح مُسرَّب؛ يدعم عناوين IP الفردية ونطاقات CIDR."
ogDescription: "أمان مفاتيح LMU AI: اضبط قوائم بيضاء وسوداء لعناوين IP لمفاتيح واجهة Claude / GPT / النماذج الصينية الخاصة بك، مع دعم لعناوين IP الفردية ونطاقات CIDR وIPv6، حتى لا يتمكن الآخرون من استخدام مفتاح مُسرَّب لاستنزاف حصتك — يتضمن استكشاف الأخطاء وأفضل الممارسات الإنتاجية."
keywords:
  - أمان مفاتيح API
  - قائمة IP البيضاء لمفاتيح API
  - منع إساءة استخدام مفاتيح API
  - ماذا تفعل عند تسرب مفتاح Claude API
  - إعداد القائمة البيضاء بصيغة CIDR
  - القائمة البيضاء لـ IPv6
  - 403 ACCESS_DENIED
  - أمان مفاتيح Claude Code
  - تقييد IP لمفاتيح LMU AI
  - حماية مفاتيح AI API
alternateNames:
  - إعداد القائمة البيضاء لعناوين IP لمفاتيح Claude API
  - دليل أمان مفاتيح LMU AI
  - إعدادات الحماية من إساءة استخدام مفاتيح API
  - كيفية الحد من الخسائر بعد تسرب مفتاح
  - دليل حماية مفاتيح Claude Code
faq:
  - q: هل يمكنني استخدام القائمة البيضاء والقائمة السوداء لعناوين IP في آنٍ واحد؟
    a: نعم. عند ضبط كليهما، تُطبَّق القائمة السوداء أولاً — حتى لو كان عنوان IP على القائمة البيضاء، فإنه يُرفض إذا طابق القائمة السوداء.
  - q: لماذا لا أستطيع الاتصال عند وضع عنوان IP خاص (شبكة محلية) في القائمة البيضاء؟
    a: لا تستطيع خوادم LMU AI رؤية عناوين IP الخاصة (مثل 192.168.x.x أو 10.x.x.x)؛ فهي ترى فقط **عنوان IP الخارج العام** الخاص بك. استخدم `curl https://ifconfig.me` للعثور على عنوان IP الخارج الحقيقي، ثم أدخله.
  - q: عنوان IP المنزلي لديّ يتغير — كيف أستخدم القائمة البيضاء؟
    a: إذا كان لديك عنوان IP عام ديناميكي، يمكنك إدخال نطاق CIDR الذي خصصه مزود الخدمة (مثل `123.45.0.0/16`) لتوسيع النطاق؛ وإذا لم تستطع تحديد النطاق، نوصي بعدم تفعيل القائمة البيضاء واستخدام حماية أخرى بدلاً منها (تحديد المعدل، الحصة الإجمالية، القائمة البيضاء للنماذج).
  - q: ماذا أفعل إذا تسرب مفتاح واجهة LMU AI API الخاص بي؟
    a: قم فوراً **بإعادة تعيين أو حذف** المفتاح في لوحة التحكم؛ وإذا كانت القائمة البيضاء لعناوين IP مضبوطة بالفعل، فلن يمكن استخدام المفتاح المُسرَّب من عناوين IP غير مألوفة، ما يقلل خسائرك بشكل كبير. نوصي أيضاً بتتبع مصدر التسرب (التزامات المستودعات العامة، السجلات، سجل المحادثات، لقطات الشاشة، إلخ) وتفعيل القائمة البيضاء لعناوين IP فوراً على المفتاح الجديد.
  - q: كم عدد عناوين IP التي يمكن أن يحملها مفتاح واحد؟ وهل يدعم IPv6؟
    a: "قاعدة واحدة لكل سطر؛ يمكنك مزج عناوين IP الفردية ونطاقات CIDR، ولا يوجد حد صارم لعدد الإدخالات. يدعم كلاً من IPv4 وIPv6 (مثال IPv6: `2001:db8::/32`). إذا كان لديك خروج بكلٍّ من v4 وv6، أدخل كليهما."
  - q: كيف أُعِدّ القائمة البيضاء للاستدعاءات من خادم إنتاجي؟
    a: أدخل **عنوان IP العام** للخادم (وليس عنوانه الخاص). إذا كنت تستخدم موازِن تحميل أو لديك عدة عناوين IP للخروج، أضف كل عناوين IP الممكنة للخروج أو نطاقات CIDR الخاصة بها إلى القائمة البيضاء؛ وللتطوير المحلي، استخدم مفتاحاً منفصلاً بدون قائمة بيضاء لتجنب التداخل.
  - q: هل تتعارض القائمة البيضاء لعناوين IP وتحديد المعدل والحصة الإجمالية؟
    a: لا، ونوصي بتكديسها معاً. تحدّد القائمة البيضاء لعناوين IP "من يمكنه الاستخدام"، ويحدّد تحديد المعدل "بأي سرعة"، وتحدّد الحصة الإجمالية "كم القدر". اضبط الثلاثة جميعاً على مفتاح الإنتاج لتحقيق دفاع متعدد الطبقات — حتى إذا تم تجاوز القائمة البيضاء، يظل تحديد المعدل والحصة يوفران خط دفاع أخير.
  - q: لماذا يُرفض استدعائي مع خطأ 403 ACCESS_DENIED؟
    a: على الأرجح لأن عنوان IP لم يطابق القائمة البيضاء. استخدم `curl https://ifconfig.me` للتحقق من عنوان IP الخارج الحقيقي، وقارنه بالقائمة البيضاء للمفتاح، ثم أضف العنوان الحالي أو امسح القائمة البيضاء مؤقتاً للاختبار؛ وإذا استمر الرفض، تحقق مما إذا كان مطابقاً للقائمة السوداء، وما إذا كان المفتاح معطلاً، وما إذا كانت الحصة قد استُنفدت.
---

إذا تسرب مفتاح، يمكن للآخرين استخدامه لاستهلاك حصتك. تُعدّ **القائمة البيضاء لعناوين IP** أكثر أشكال الحماية مباشرةً — فقط عناوين IP التي تثق بها مسموح لها باستخدام المفتاح.

## أين تضبطها

اذهب إلى **لوحة التحكم ← مفاتيح API ← تعديل (أو إنشاء)** وابحث عن الحقلين، **القائمة البيضاء لعناوين IP** و**القائمة السوداء لعناوين IP**.

قاعدة واحدة لكل سطر، بأحد صيغتين:

| الصيغة | مثال | الوصف |
|------|------|------|
| عنوان IP فردي | `203.0.113.10` | يطابق عنوان IP واحداً بالضبط |
| نطاق CIDR | `203.0.113.0/24` | يطابق نطاقاً كاملاً من عناوين IP (`/24` = 256 عنواناً) |

يُدعم كلٌّ من IPv4 وIPv6 (مثال IPv6: `2001:db8::/32`).

## كيف تُطبَّق

- **القائمة البيضاء فارغة** → لا قيود على المصدر؛ يمكن لأي عنوان IP استخدام المفتاح
- **القائمة البيضاء غير فارغة** → فقط عناوين IP على القائمة يمكنها الاستخدام؛ وكل ما عداها يُرفض
- **القائمة السوداء** → عناوين IP المطابقة تُرفض، **بأولوية على القائمة البيضاء**
- يمكنك استخدام كليهما معاً (تُفحص القائمة السوداء أولاً، ثم القائمة البيضاء)

عند الرفض، تُرجع الواجهة `403 ACCESS_DENIED`.

## اعثر على عنوان IP الخارج الحقيقي (مهم!)

**هذا هو الخطأ الأسهل ارتكاباً**: يجب أن يكون عنوان IP الذي تضبطه على المفتاح هو **عنوان IP الخارج العام** الذي تراه خوادم LMU AI، وليس العنوان الخاص الذي يظهره `ipconfig` / `ifconfig` على جهازك.

```bash
# Command line
curl https://ifconfig.me
# or
curl https://ip.sb
```

أو افتح [ip.sb](https://ip.sb) / [ifconfig.me](https://ifconfig.me) في متصفحك.

<Callout type="warn">
  يتغير عنوان IP الخارج عند تشغيل شبكة VPN أو وكيل شركة، أو عندما يبدّل جهازك المحمول شبكات Wi-Fi. تأكد منه بالأمر أعلاه قبل ضبط القائمة البيضاء.
</Callout>

## سيناريوهات شائعة

**السيناريو 1: عنوان IP مكتبي ثابت**
```
Whitelist:
203.0.113.10
```

**السيناريو 2: العمل من المنزل والمكتب معاً**
```
Whitelist:
203.0.113.10      # Company egress
198.51.100.20     # Home broadband
```

**السيناريو 3: التشغيل على خادمك السحابي الخاص**
```
Whitelist:
<your cloud server's public IP>
```
اسمح فقط لخادمك باستدعاء الواجهة، واستخدم مفتاحاً منفصلاً (بدون قائمة بيضاء) للتطوير المحلي.

**السيناريو 4: تريد فقط حظر عنوان IP مشبوه مؤقتاً**
```
Blacklist:
1.2.3.4
```
كل شيء آخر يعمل كالمعتاد، دون أي تأثير على الاستخدام الطبيعي.

## استكشاف الأخطاء

**س: يُرجع استدعائي 403 ACCESS_DENIED — هل هو تقييد IP؟**

على الأرجح. تحقق بالترتيب:

1. استخدم `curl https://ifconfig.me` لتأكيد عنوان IP الخارج الحقيقي الحالي
2. قارنه بالقائمة البيضاء للمفتاح لمعرفة ما إذا كان يطابق
3. إذا لم يطابق، أضف العنوان الحالي إلى القائمة البيضاء، أو امسح القائمة البيضاء مؤقتاً للاختبار
4. إذا استمر الرفض، تحقق مما إذا كان على القائمة السوداء، وما إذا كان المفتاح معطلاً، وما إذا كانت الحصة قد استُنفدت

**س: وضعت عنوان IP في القائمة البيضاء لكنني ما زلت أُرفَض؟**

- هل أدخلت **عنوان IP خاصاً** (`192.168.x.x` / `10.x.x.x` / `172.16-31.x.x`)؟ استبدله بعنوان IP الخارج العام الخاص بك.
- هل أدخلت عنوان IPv6 لكنك تتصل عبر IPv4 (أو العكس)؟ أدخل كليهما.
- هل أدخلت CIDR بصيغة خاطئة (مثل `1.2.3.4/33`)؟ القواعد غير الصالحة تُتجاهل بصمت.

## ممارسات موصى بها

- بالنسبة إلى **مفاتيح الإنتاج**، نوصي بشدة بضبط قائمة بيضاء لعناوين IP لتقليل سطح الهجوم
- بالنسبة إلى **مفاتيح التطوير / التصحيح**، يمكنك تخطي القائمة البيضاء، لكن يجب ضبط **حد أقصى للحصة الإجمالية** كخط دفاع أخير
- إذا كنت قلقاً بشكل خاص من التسريبات، اجمعها مع [**تحديد المعدل** و**الحصة الإجمالية** و**القائمة البيضاء للنماذج**](/ar/docs/guide/how-to-use-token) لدفاع متعدد الطبقات
- في اللحظة التي تشك فيها بتسرب مفتاح، **أعد تعيينه أو احذفه** في لوحة التحكم فوراً
